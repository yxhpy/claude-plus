# Codex 插件使用指南

## ✅ 插件已修复并可以使用！

插件现在已经正确配置并通过验证。

## 🚀 如何使用

### 1. 加载插件

```bash
cd /path/to/your/project
claude --plugin-dir /path/to/codex-plugin
```

### 2. 可用的技能命令

插件提供以下技能（slash commands）：

| 命令 | 用途 | 示例 |
|------|------|------|
| `/codex:quick-review` | 快速代码审查 | `/codex:quick-review` |
| `/codex:apply` | 应用 Codex 差异 | `/codex:apply` |
| `/codex:resume-session` | 恢复会话 | `/codex:resume-session` |
| `/codex:exec-command` | 执行任务 | `/codex:exec-command analyze code` |
| `/codex:exec` | 自动执行（代理调用） | Claude 自动使用 |
| `/codex:review` | 代码审查（代理调用） | Claude 自动使用 |

### 3. 使用示例

#### 快速代码审查
```bash
# 在 Claude Code 中
/codex:quick-review

# 或者直接问 Claude
"Can you review my code for security issues?"
```

#### 执行任务
```bash
/codex:exec-command run all tests
```

#### 应用更改
```bash
/codex:apply
```

## ✅ 验证插件

使用 Claude Code 的内置验证：

```bash
claude plugin validate ./codex-plugin
```

应该看到：
```
✔ Validation passed
```

## 🧪 测试插件

```bash
# 测试快速审查
claude --plugin-dir ./codex-plugin -p "/codex:quick-review" --print

# 测试执行命令
claude --plugin-dir ./codex-plugin -p "/codex:exec-command analyze project" --print
```

## 📁 插件结构

```
codex-plugin/
├── .claude-plugin/
│   └── plugin.json           # 插件清单 ✅
└── skills/                    # 技能目录 ✅
    ├── quick-review/
    │   └── SKILL.md          # 快速审查技能
    ├── apply/
    │   └── SKILL.md          # 应用差异技能
    ├── resume-session/
    │   └── SKILL.md          # 恢复会话技能
    ├── exec-command/
    │   └── SKILL.md          # 执行命令技能
    ├── exec/
    │   └── SKILL.md          # 执行技能（代理）
    └── review/
        └── SKILL.md          # 审查技能（代理）
```

## 🔧 修复的问题

1. ✅ **plugin.json 格式错误** - `repository` 字段从对象改为字符串
2. ✅ **目录结构错误** - 将 `commands/` 改为 `skills/` 目录
3. ✅ **SKILL.md 缺少 name 字段** - 为所有技能添加了 `name` 字段
4. ✅ **通过 Claude 官方验证** - `claude plugin validate` 通过

## 💡 提示

### 用户调用 vs 代理调用

- **用户调用技能**（如 `quick-review`, `apply`, `resume-session`, `exec-command`）
  - 用户使用 `/codex:skill-name` 显式调用
  - 适合直接操作

- **代理调用技能**（如 `exec`, `review`）
  - Claude 根据上下文自动调用
  - 用户只需自然语言描述需求

### 示例对话

```
用户: "Can you review my code for security issues?"
Claude: [自动使用 review 技能]

用户: "/codex:quick-review security"
Claude: [执行快速安全审查]
```

## 📦 安装选项

### 临时使用（推荐测试）
```bash
claude --plugin-dir ./codex-plugin
```

### 永久安装
```bash
cp -r codex-plugin ~/.claude/plugins/
claude
```

### 项目级安装
```bash
# 提交到项目
git add codex-plugin
git commit -m "Add Codex plugin"

# 团队成员使用
claude --plugin-dir ./codex-plugin
```

## 🎉 现在可以使用了！

插件已经修复并通过验证，可以正常使用。试试这个命令：

```bash
claude --plugin-dir ./codex-plugin
```

然后在 Claude Code 中输入：
```
/codex:quick-review
```

祝使用愉快！🚀
